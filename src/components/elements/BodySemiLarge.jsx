const BodyLarge = ({children, className}) => {
    return (
        <span className={`font-semibold text-sm leading-[140%] tracking-[0.2px] ${className}`}>{children}</span>
    )
}
export default BodyLarge